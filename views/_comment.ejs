<li>
    <p>
        <!-- && locals.user.id == comment.user.id -->
        <% if(locals.user) { %>
            <small>
                <a href="/comments/destroy/<%= comment.id %>">X</a>
            </small>
        <% } %>

        <%= comment.content %>
        <br>
        <small>
            <%= comment.user.name %>
        </small>
    </p>
</li>